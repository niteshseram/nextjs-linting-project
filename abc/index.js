//declare a variable but not used it anywhere in this file
//should be caught by ESLint after abc directory being added in next.config.js
const abc=1
const Home = () => (
  <div>Hell\o</div>
)

export default Home